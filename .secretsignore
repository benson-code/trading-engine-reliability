# check-no-secrets.sh 的例外清單
#
# 格式：<檔案路徑>:<行號>  # 理由
# 每一條都必須寫理由 —— 沒有理由的例外就是漏洞。
# 語法：以 # 開頭為註解，空行忽略。

payment-api/src/test/java/com/binance/payment/api/PaymentAuthTest.java:25  # 測試夾具：驗證 X-API-Key 認證用的假金鑰，不對應任何真實系統
